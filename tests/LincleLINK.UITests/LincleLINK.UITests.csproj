<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
    <PackageReference Include="FluentAssertions" />
    <PackageReference Include="Appium.WebDriver" />
    <!-- Generates .torrent fixtures for the torrent pre-fill wizard tests. -->
    <PackageReference Include="MonoTorrent" />
  </ItemGroup>

  <!-- Black-box tests: the reference is only there so building the tests builds
       (and copies) the app exe the Appium session launches. -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\LincleLINK.App\LincleLINK.App.csproj" />
  </ItemGroup>

</Project>
